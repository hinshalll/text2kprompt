I have carefully reviewed your `appd1_6.py` code[cite: 2]. You are right—some of the earlier code snippets didn't match your exact file because your app has grown quite complex.

Here is the **complete, surgeon-precise guide** to implementing the Agentic Architecture into your specific code. Follow these steps one by one.

---

### Step 1: Add the Parallel Engine (Top of File)
We need to tell Python to allow multiple AI "brains" to work at once.

🔍 **FIND (Line 2):**
```python
from datetime import date, datetime, time, timedelta
```
✅ **REPLACE WITH:**
```python
import concurrent.futures # 🚀 Allows parallel AI agents
from datetime import date, datetime, time, timedelta
```

---

### Step 2: Update Your Model List
We are swapping the low-limit models for the high-accuracy Gemma 4 team.

🔍 **FIND (Around Line 160):**
```python
FREE_MODELS = [
    "gemini-3-flash-preview",
    "gemini-3.1-flash-lite-preview",
    "gemini-2.5-flash"
]
```
✅ **REPLACE WITH:**
```python
FREE_MODELS = [
    "gemma-4-31b",                   # The Reader (Strict Rules)
    "gemma-4-26b",                   # The Philosopher (Synthesis & UI)
    "gemini-3.1-flash-lite-preview"  # The Speed Calculator (KP Math)
]
```

---

### Step 3: Upgrade the AI Helper Functions
We need a way to call specific models for specific tasks.

🔍 **FIND AND HIGHLIGHT EVERYTHING (From Line 186 to 216):**
*Highlight everything from `def get_ai_model(model_idx=0):` down to the end of `def generate_content_with_fallback(...):`*

✅ **REPLACE THAT ENTIRE BLOCK WITH THIS:**
```python
def get_ai_model_by_name(model_name, custom_system_rules=None):
    """Directly calls a specific model with dynamic system rules."""
    system_rules = custom_system_rules or """<ROLE>You are an elite, highly precise Vedic Astrologer, Numerologist, and Tarot Reader.</ROLE>
    <KNOWLEDGE_BASE_DIRECTIVES>
    1. Your interpretive rules MUST come entirely from the attached Markdown files.
    2. Treat all numbers as absolute, unchangeable facts.
    </KNOWLEDGE_BASE_DIRECTIVES>"""
    safe_config = [{"category": "HARM_CATEGORY_HARASSMENT", "threshold": "BLOCK_NONE"},
                   {"category": "HARM_CATEGORY_HATE_SPEECH", "threshold": "BLOCK_NONE"},
                   {"category": "HARM_CATEGORY_SEXUALLY_EXPLICIT", "threshold": "BLOCK_NONE"},
                   {"category": "HARM_CATEGORY_DANGEROUS_CONTENT", "threshold": "BLOCK_NONE"}]
    return genai.GenerativeModel(model_name=model_name, system_instruction=system_rules, safety_settings=safe_config)

def agent_worker(prompt, file_objs, model_id):
    """A thread-safe AI agent that extracts factual notes without crashing."""
    if not isinstance(file_objs, list): file_objs = [file_objs]
    try:
        model = get_ai_model_by_name(model_id)
        return model.generate_content(file_objs + [prompt]).text
    except Exception as e:
        return f"Agent Note: Data extraction failed ({str(e)}). Please infer from raw dossier."

def generate_content_with_fallback(prompt, knowledge_files=None, preferred_model="gemma-4-26b"):
    """Global fallback wrapper that prioritizes the MoE model for UI speed."""
    content_to_send = knowledge_files + [prompt] if knowledge_files else [prompt]
    models_to_try = [preferred_model] + [m for m in FREE_MODELS if m != preferred_model]
    for m_name in models_to_try:
        try:
            return get_ai_model_by_name(m_name).generate_content(content_to_send).text
        except Exception as e:
            if "429" in str(e) or "quota" in str(e).lower(): continue
            raise e 
    raise Exception("All free models limits reached.")
```

---

### Step 4: Add Agent Prompt Builders
These are the specialized instructions for your "team of experts."

🔍 **FIND (Line 414):**
```python
def build_deep_analysis_prompt(dossier):
```
✅ **ADD THESE 4 NEW FUNCTIONS DIRECTLY ABOVE IT:**
```python
def build_agent_parashari_prompt(dossier):
    return f"""<mission>Using ONLY bphs1.md, extract factual bullet points for: Core Identity (Lagna Lord), Mental World (Moon Avastha), and Health (H6/H8).</mission>\n<user_chart_data>{dossier}</user_chart_data>"""

def build_agent_timing_prompt(dossier):
    return f"""<mission>Using ONLY bphs2.md, analyze the VIMSHOTTARI DASHA table and SADE SATI. Identify exactly which phase they are in and what the next 2 sub-periods bring.</mission>\n<user_chart_data>{dossier}</user_chart_data>"""

def build_agent_kp_prompt(dossier):
    return f"""<mission>Using ONLY kp3.md, read the HOUSE STRENGTH SUMMARY. Explain why Career (H10) and Wealth (H2) are 'promised' or 'not promised'.</mission>\n<user_chart_data>{dossier}</user_chart_data>"""

def build_master_synthesizer_prompt(dossier, p_notes, t_notes, k_notes):
    return f"""<mission>You are the Master Astrologer. Synthesize these specialist notes into a final, flowing, human reading using iva.md.
    <notes_p>{p_notes}</notes_p> <notes_t>{t_notes}</notes_t> <notes_k>{k_notes}</notes_k>
    Structure: 1. Identity. 2. Mind. 3. Career. 4. Wealth. 5. Relationships. 6. Timing. 7. Remedies.</mission>\n<user_chart_data>{dossier}</user_chart_data>"""
```

---

### Step 5: Implement Parallel Analysis (Oracle)
We now wire the "Full Reading" button to fire all 3 agents.

🔍 **FIND AND HIGHLIGHT (Lines 688-689):**
```python
            elif mission=="Deep Personal Analysis":
                final=build_deep_analysis_prompt(generate_astrology_dossier(profiles[0],d60s[0]))
```
✅ **REPLACE THOSE 2 LINES WITH THIS:**
```python
            elif mission=="Deep Personal Analysis":
                dossier = generate_astrology_dossier(profiles[0], d60s[0])
                st.info("🧠 Firing Parallel AI Agents (Takes ~20s)...")
                agent_files = get_knowledge_files(["bphs1.md", "bphs2.md", "kp3.md"])
                
                with concurrent.futures.ThreadPoolExecutor(max_workers=3) as executor:
                    f_p = executor.submit(agent_worker, build_agent_parashari_prompt(dossier), agent_files[0], "gemma-4-31b")
                    f_t = executor.submit(agent_worker, build_agent_timing_prompt(dossier), agent_files[1], "gemma-4-31b")
                    f_k = executor.submit(agent_worker, build_agent_kp_prompt(dossier), agent_files[2], "gemini-3.1-flash-lite-preview")
                    p_notes, t_notes, k_notes = f_p.result(), f_t.result(), f_k.result()
                    
                final = build_master_synthesizer_prompt(dossier, p_notes, t_notes, k_notes)
```

---

### Step 6: The "Infinite" Chat Upgrade (Consultation Room)
This rewrites the chat to prevent context crashes.

🔍 **FIND AND HIGHLIGHT EVERYTHING (From Line 545 to 595):**
*Highlight the entire `def show_consultation_room():` function.*

✅ **REPLACE IT WITH THIS:**
```python
def show_consultation_room():
    components.html("""<script>setTimeout(function(){var b=window.parent.document.querySelector('button[aria-label="Collapse sidebar"]');if(b&&window.parent.innerWidth<=768)b.click();},80);</script>""",height=0,width=0)
    st.markdown("<h1>💬 Ask the Astrologer</h1>", unsafe_allow_html=True)
    dp, _ = get_default_profile()
    if not dp: st.warning("Please set a ⭐ default profile first."); return
    
    memory_key = f"v2_chat_{dp['name']}"
    if memory_key not in st.session_state: st.session_state[memory_key] = []
    
    # UI History
    for msg in st.session_state[memory_key]:
        with st.chat_message("assistant" if msg["role"] == "model" else "user"):
            st.markdown(msg["display"])

    if q := st.chat_input("Ask anything..."):
        st.chat_message("user").markdown(q)
        with st.chat_message("assistant"):
            res_ph = st.empty()
            with st.spinner("Consulting books..."):
                dos = generate_astrology_dossier(dp)
                books = get_knowledge_files(["bphs1.md", "bphs2.md", "kp3.md"])
                
                # PARALLEL FETCHING OF FACTS PER MESSAGE
                with concurrent.futures.ThreadPoolExecutor(max_workers=3) as executor:
                    f_p = executor.submit(agent_worker, f"Extract identity facts for: {q}", books[0], "gemma-4-31b")
                    f_t = executor.submit(agent_worker, f"Extract timing facts for: {q}", books[1], "gemma-4-31b")
                    f_k = executor.submit(agent_worker, f"Extract math facts for: {q}", books[2], "gemini-3.1-flash-lite-preview")
                    notes = f"PARASHARI: {f_p.result()}\nTIMING: {f_t.result()}\nKP: {f_k.result()}"

                # SYNTHESIS (History is kept slim)
                hist = [{"role": m["role"], "parts": [m["internal"]]} for m in st.session_state[memory_key]]
                model = get_ai_model_by_name("gemma-4-26b", custom_system_rules=f"You are a warm Astrologer. Rely on these Agent Notes:\n{notes}")
                chat = model.start_chat(history=hist)
                response = chat.send_message(q, stream=True)
                full_txt = ""
                for chunk in response: full_txt += chunk.text; res_ph.markdown(full_txt + "▌")
                res_ph.markdown(full_txt)
                st.session_state[memory_key].append({"role": "user", "display": q, "internal": q})
                st.session_state[memory_key].append({"role": "model", "display": full_txt, "internal": full_txt})
```

---

### Step 7: Fix the Synthesis Routing
Finally, ensure the Synthesis stage in the Oracle uses the Philosopher model.

🔍 **FIND (Around Line 715):**
```python
stream_ai_with_followup(st.session_state[f"oracle_prompt_{mission}"], f"oracle_{mission}_history", "The Oracle is channeling the stars...", knowledge_files=oracle_files)
```
✅ **REPLACE WITH:**
```python
stream_ai_with_followup(st.session_state[f"oracle_prompt_{mission}"], f"oracle_{mission}_history", "The Master Astrologer is writing...", knowledge_files=oracle_files, preferred_model="gemma-4-26b")
```

**That is everything.** Save your file and restart the app. You now have a state-of-the-art Agentic Astrology AI[cite: 2].